# MD-04 — Deployment Finalization (Render-Ready)

## Purpose
Make the project trivially deployable to a free-tier platform (Render or Railway)
with no code changes after this step.

## In Scope
- Add minimal deployment documentation (README or DEPLOY.md)
- Specify required environment variables
- Ensure backend start command is explicit and production-safe
- Ensure frontend build/start commands are clear

## Out of Scope
- No Docker required
- No CI/CD
- No infra automation
- No new code features

## Requirements

### Backend
- Provide a production start command (uvicorn / gunicorn)
- Document required env vars (CORS_ORIGINS)

### Frontend
- Document NEXT_PUBLIC_API_URL usage
- Ensure build works with defaults

### Documentation
- Clear, copy-pasteable steps for:
  - Render
  - Railway (optional if identical)

## Success Criteria
- A new user can deploy in <10 minutes
- No code edits needed to deploy
- Defaults remain local-dev friendly
